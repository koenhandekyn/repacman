css:
  .home_actions a.btn {
    padding: 1.5rem 3rem;
    text-transform: uppercase;
    background-color: #327756;
    display: block;
    margin-bottom: 1rem;
  }

  h1.welcome {
    font-size: 2.5rem;
    margin-bottom: 2rem;
    font-weight: 400;
  }

  section {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 1rem;
    align-items: center;
    /* center each column */
    justify-items: center;
  }



section.

  .home_actions
    h1.welcome Welkom
    a.btn href=href=new_batch_path Nieuwe productie
    a.btn href=export_printeds_path(format: :xml)
      | Export XML

  .images
    img src=asset_path("horse.jpg") alt="Horse" height="400"

javascript:
  document.getElementById("export-btn").addEventListener("click", () => {
    setTimeout(() => { location.reload(); }, 1000); // Adjust the timeout as needed
  });
